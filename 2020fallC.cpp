//#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <map>
#include <queue>
#include <random>
#include <set>
#include <stack>
#include <string>
#include <unordered_map>
#include <vector>
using namespace std;

typedef long long ll;

//const int mod = 998244353;
const int mod = 1e9 + 7;
const int maxn = 1e5 + 5;

struct node {
    int l, r, v, sum;
} t[maxn * 40];

int tot = 0;
class Solution {
public:
    void insert (int l, int r, int &now, int pre, int val) {

        
    }
    
    ll que (int ) {


    }
    
    vector<int> numsGame(vector<int>& nums) {
        vector<int> v;
        int n = nums.size();
        for (int i = 0; i < nums.size(); i++) {
            v.push_back(nums[i] - i);
        }
        
        
    }
};
